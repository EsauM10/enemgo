<div class="row justify-content-center my-auto mx-0">
  <div class="col-lg-8">
    <%= form_for(@user, url: wizard_path,
      method: :put, html: { class: "was-validated card p-3" }) do |form| %>
      <div class="card-title brd-b-1">
        <h4 class="text-default">Complete seu cadastro</h4>
      </div>

      <%= progress_bar %>

      <%= yield form %>

      <div class="card-footer d-flex justify-content-between">
        <%= link_to 'Pular', next_wizard_path, class: "text-muted" %>
        <%= form.submit 'Próximo', class: "btn btn-round btn-outline-primary" %>
      </div>

    <% end %>

  </div>
</div>
