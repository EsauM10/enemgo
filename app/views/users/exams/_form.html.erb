<%= form_with(url: exams_path, model: [current_user.module_kind, exam], local: true) do |form| %>
  <% if exam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exam.errors.count, "error") %> prohibited this exam from being saved:</h2>

      <ul>
      <% exam.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year, id: :exam_year %>
  </div>

  <div class="field">
    <%= form.label :kind %>
    <%= form.number_field :kind, id: :exam_kind %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.number_field :status, id: :exam_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
